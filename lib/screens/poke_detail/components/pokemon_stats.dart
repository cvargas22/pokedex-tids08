import 'package:flutter/material.dart';


class PokemonStats extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Stats Pokemon'),
    );
  }
}