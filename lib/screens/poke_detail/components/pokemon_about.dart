import 'package:flutter/material.dart';


class PokemonAbout extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Sobre el Pokemon'),
    );
  }
}